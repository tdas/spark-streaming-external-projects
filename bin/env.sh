#!/bin/bash

# Configuration
export SPARK_VERSION="1.1.0"  # If the specific binary url is not specified then this is the version that is going to be used to download the binaries
export SPARK_GIT_URL="https://github.com/apache/spark"
export SPARK_GIT_BRANCH="branch-1.0"
